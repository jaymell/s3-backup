Tar, gzip and send files to s3

## TODO
* Delete tar file before exiting if exception occurs
* clean up bloated main function
* Add manifest file to archive -- requires ability to pull out file names from recursive adds
